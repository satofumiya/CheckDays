            <h1><strong>「<%= @goal.text %>」</strong>  を達成するためのサブゴール一覧</h1>
            <%= link_to "サブゴールを追加する", new_goal_subgoal_path, class:"btn btn-primary m-3" %>

        <% @subgoals.each do |subgoal| %>
          <div class="stream-item">
           <div class="content d-flex align-items-center">
              <div class="goal-text my-4 mx-2 col-7 border-1 p-2">
                <%= link_to subgoal.title, goal_subgoal_path(@goal,subgoal)%>  
              </div>
               <div class="mx-2 col-2">
                <%= link_to  "サブゴール編集画面へ", edit_goal_subgoal_path(@goal, subgoal), class:"btn btn-primary" %>
              </div>
              <div class="d-flex justify-content-end my-2 ">
                <%= link_to 'サブゴールを削除する', goal_subgoal_path(@goal, subgoal),method: :delete, data: {confirm: "削除しますか？"},class:'btn btn-danger' %>
              </div>
            </div>
          </div>
        <% end %>